package service.room;

import domainModell.room.Room;
import service.ServiceRunner;

public class Main {
    public static void main(String[] args) {
     /*       FindAllRoomService findAllRoomService = new FindAllRoomService();
        List<Room> roomList = findAllRoomService.execute();
        for(Room room : roomList){
            System.out.println("Id: "+room.getId()+
                    " Number: "+room.getRoomNumber()+
                    " Area: "+room.getRoomArea()+
                    " Type: "+room.getRoomType());
        }*/
    /*    int id = 47;
        DeleteRoomByIdService deleteByIdService = new DeleteRoomByIdService(id);
        boolean room = deleteByIdService.execute();
        System.out.println(room);
*/
 /*       UpdateRoomService updateService = new UpdateRoomService(new Room(46, 10, 10, "LIVINGROOM/*"));
        updateService.execute();
*/
    /*    SaveRoomService saveRoomService = new SaveRoomService(new Room(1, 8, 88.8, "WAREHOUSE"));
        ServiceRunner controllService = new ServiceRunner(commandService);
        controllService.setCommand(saveRoomService);
        controllService.executeService();*/
    }

}
